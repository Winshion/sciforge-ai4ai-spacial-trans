# Reproduction Contract

Selected experiment: MCFST spatial domain identification on the 10x Visium human breast cancer dataset, Section 2.3 / Fig. 3A. Paper target: ARI = 0.693 against 20 manually annotated subregions.

Required reporting: provenance-backed code/data, fixed-seed runs, KMeans and Louvain baselines, saved predictions/metrics/plots, ARI verification, reviewer audit, final verdict exactly one of reproduced/partially reproduced/failed.

Recovery note: the parent-provided `.git_repro`, full contract, and local preprocessed snapshot were present at initial inspection but disappeared during execution without any destructive command from this agent. This run proceeds from the official GitHub repository raw data and records this blocker in provenance and reports.
